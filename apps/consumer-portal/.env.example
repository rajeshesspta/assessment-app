# Copy to .env.local to override defaults for Vite dev server
VITE_HOST=0.0.0.0
VITE_PORT=5173
# Override to point the dev proxy at a different Consumer BFF host.
# Leave blank to disable proxying entirely.
VITE_PROXY_API=http://localhost:4000
VITE_CONSUMER_BFF_URL=http://localhost:4000
